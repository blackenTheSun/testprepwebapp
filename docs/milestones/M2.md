# M2: Rendering and handoff (complete, with manual QA items open)

**SOW gate:** local LaTeX, typed 2D and constrained 3D visual adapters, derivative action, fixtures, cross-browser QA, source and README.

| Gate item | Status | Evidence |
| --- | --- | --- |
| Local LaTeX | Done | KaTeX bundled and fonts inlined (D10); `trust: false`, and invalid LaTeX renders red instead of crashing. |
| 2D adapter | Done | `Primitive2D` subclass for each of the 10 kinds ([src/render/diagram2d.tsx](../../src/render/diagram2d.tsx)). Unit test renders every kind; E2E **AC4** checks the bolt-group diagram (3 plates, 4 bolts, load label). |
| Constrained 3D adapter | Done | `Camera` + `Object3D` subclass for each of the 8 kinds + `SceneProjector` ([src/render/scene3d.tsx](../../src/render/scene3d.tsx)): SVG projection, back-face culling, depth sort, auto-fit (D30–D33). E2E **AC4** checks the axial bar: 3 visible box faces, support plane, axes labels x/y/z, "steel bar", "fixed support", "P = 60 kN", and alt text. |
| Derivative action | Done | `DerivativeOperation` creates a symbolic variable (`resultLatex`) and a numeric one (`numericEvaluation`). The step card shows the stored explanation. E2E **AC8**. |
| Fixtures | Done | Both reference files are bundled and exercised end-to-end. |
| Automated acceptance tests | Done | [tests/e2e/acceptance.spec.ts](../../tests/e2e/acceptance.spec.ts): AC1–AC10 against `file://…/dist/index.html` with no network. Passing in Chromium and Edge locally and in Chromium, Firefox and WebKit in CI. |
| Cross-browser QA | Automated: done. Manual: open | See below. |
| Source, README | Done | [README.md](../../README.md) (build/test/release), [AMY_QUICKSTART.md](../AMY_QUICKSTART.md), [TEST_FILE_GUIDE.md](../TEST_FILE_GUIDE.md) (field guide + ChatGPT prompt), [ARCHITECTURE.md](../ARCHITECTURE.md), [DECISIONS.md](../DECISIONS.md). |

## Test totals at completion

- Unit: 41 tests in 6 files (Vitest), all passing.
- E2E: 7 tests covering AC1–AC10. Locally they pass in Chromium and Edge; CI passes all 21 runs (Chromium, Firefox, WebKit).

## Cross-browser status

| Browser | How checked | Result |
| --- | --- | --- |
| Chromium | Playwright, local + CI | Pass |
| Microsoft Edge (installed) | Playwright `--project=msedge`, local | Pass |
| Firefox | Playwright in CI (Linux) | Pass: 7/7 ([CI run 35971462534](https://github.com/blackenTheSun/testprepwebapp/actions/runs/35971462534)) |
| WebKit (Safari engine) | Playwright in CI (Linux) | Pass: 7/7, same run. WebKit runs without Playwright's offline flag, which breaks `file://` navigation there; http(s) is blocked by a route rule and the CSP instead (D37) |
| Real Firefox on Windows, real Safari on macOS | Manual | **Open:** double-click `index.html`, load both examples, and complete one problem |

## Screens reviewed

Home, problem list, the bolt-group 2D diagram, the axial-bar 3D scene, the bilinear stress–strain diagram, a worked Add Step flow, and the review, in light and dark themes at 1360×900. Fixes made from that review: the selected-button hover contrast, the diagram label placement rules (D35: clipped source label, axis titles on the plot, label collisions in the stress–strain diagram), and the formula sheet ordering (D25).

## Remaining before final acceptance

1. Manual double-click check in real Firefox (Windows) and Safari (macOS).
2. Brent's review of the auto-naming conventions (D21) and diagram label formatting (D34).
3. The SOW's 14-day defect window starts at acceptance.
