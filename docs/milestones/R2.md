# R2 (v0.3 delta): acceptance, QA and docs (complete, manual QA items open)

**Plan gate** ([IMPLEMENTATION_PLAN_V0.3.md §6](../IMPLEMENTATION_PLAN_V0.3.md)): all delta acceptance checks green in CI; the v1 E2E suite unchanged and green.

## SOW v0.3 §8 acceptance checks

All in [tests/e2e/rapidChecks.spec.ts](../../tests/e2e/rapidChecks.spec.ts), run against the built `dist/index.html` over `file://` with the network blocked. **CI run [36090758698](https://github.com/blackenTheSun/testprepwebapp/actions/runs/36090758698): 45/45 passed** (15 tests × Chromium, Firefox, WebKit). Locally they also pass in Chromium and Microsoft Edge.

| §8 check | Test | What it demonstrates |
| --- | --- | --- |
| Legacy compatibility | "Legacy compatibility…" plus the unchanged [acceptance.spec.ts](../../tests/e2e/acceptance.spec.ts) (AC1–AC10) | A v1 file opens with no rapid-check area; timer, dropdown steps and solution reveal behave as before |
| Rapid-only support | "Rapid-only support…" | [rapid-only.json](../../tests/fixtures/v2/rapid-only.json) (no problems, formulas or conversions) opens with no error or placeholder problem and starts a check |
| Offline visual asset | "Offline visual asset…" | A typed 3D scene, an embedded **JPEG** (in a side-by-side pair) and an embedded **PNG** render from `data:` URLs built from the file, with authored alt text (`alt` + "Describe this picture"); natural sizes 200 and 400 px; zero non-file requests |
| Visual closed answers | "Visual closed answers…" | Identification scores wrong then right against the authored option; a wrong true/false shows "The statement is False." plus the authored correction |
| Diagram label match | "Diagram label match…" | 5 visible markers, 5 dropdowns, 8 terms (3 decoys); a used term disappears from the other dropdowns; Submit waits for a complete one-to-one mapping; "Partly correct: 4 of 5"; the overlay shows ✓/✗ with correct terms on all 5 markers |
| Rapid visual set | "Rapid visual set…" | 8 cards in authored order; visible countdown (8 s → 5 s); card 1 expires → Show answer; card 2 expires → Next card (review at end); both appear under Unanswered in the summary; Retry round 2 starts with card 1 |
| Deliberate recall | "Deliberate recall…" | No text box, canvas or Submit; Reveal reference shows 3 key points and the reference scene; only Got It / Review; Review lands in "Marked Review" |
| Safe math and errors | "Safe math and errors…" | The placeholder's `sin` (degrees) formula computes h = 2 m through the normal dropdown step; five invalid v2 files ([tests/fixtures/invalid-v2/](../../tests/fixtures/invalid-v2/)) each show a readable error: bad asset, invalid callout, undersized label bank, duplicate answer target, unsupported item type |

## Test totals at completion

- Unit: **78** tests in 8 files (Vitest), all passing.
- E2E: **15** tests (7 v0.2 + 8 v0.3), all passing in Chromium, Firefox and WebKit in CI, and in Chromium and Edge locally.

## SOW §7 delivered work

| Deliverable | Where |
| --- | --- |
| Visual schema and guidance | [test-file.local.v2.schema.json](../handoff/test-file.local.v2.schema.json), [TEST_FILE_GUIDE.md §9](../TEST_FILE_GUIDE.md#9-v2-visual-rapid-checks) (fields, callouts, pictures, trig, author checklist, ChatGPT prompt), asset guardrails (D43), `npm run embed-image` |
| Visual runner | R1: identification, true/false, comparison, dropdown matching, reveal overlays, immediate and end feedback, rapidVisual per-card timing with the expiry choice |
| Safe visual support | Typed scenes, callouts, highlights, side-by-side panels, embedded PNG/JPEG, trig with explicit units, the sphere primitive, wireframe boxes |
| Proof fixture | [placeholder-visual-checks.test.example.json](../handoff/v0.3/placeholder-visual-checks.test.example.json), generic placeholder content (D49) meeting every §7 minimum (unit-tested) |
| Verification | This report; [R0](R0.md) and [R1](R1.md); the guide's checklist |

## Screens reviewed

Light and dark, 1280 px: home (checks and problems), identification with highlight, the pair, matching feedback overlay, recall reveal, summary, rapid countdown, and expired + Show answer. Fixes made: the cube camera angle, badge colours leaking onto summary cards, and pair caption alignment.

## Remaining before final acceptance

1. **Replace the placeholder fixture** with client-approved course content (for example BCC/FCC unit cells, Bragg geometry, Miller planes). The contract, guide and `embed-image` are ready.
2. Manual double-click check in real Firefox (Windows) and Safari (macOS): load the placeholder example, run one rapid round, and let a card expire.
3. Brent's review of the rapid-check decisions D51–D59, especially the expiry flow and the explicit Submit on true/false cards.

## Decisions

D60–D62 in [DECISIONS.md](../DECISIONS.md).
