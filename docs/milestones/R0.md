# R0 (v0.3 delta): v2 contract and validation (complete)

**Plan gate** ([IMPLEMENTATION_PLAN_V0.3.md §6](../IMPLEMENTATION_PLAN_V0.3.md)): v1 suite still green; every SOW §3 rejection has a failing-fixture test; trig evaluates in degrees and radians.

| Item | Status | Evidence |
| --- | --- | --- |
| Contract registry, v1 frozen | Done | [src/contract/contracts.ts](../../src/contract/contracts.ts): `ContractV1`, `ContractV2`, `ContractRegistry`, `TestFileValidator`. The v1 schema file is unchanged; all 41 original unit tests and all 7 v1 E2E tests pass (Chromium + Edge). |
| v2 schema | Done | Composed by [scripts/compose-v2-schema.mjs](../../scripts/compose-v2-schema.mjs) into [src/contract/test-file.local.v2.schema.json](../../src/contract/test-file.local.v2.schema.json) and the identical published copy [docs/handoff/test-file.local.v2.schema.json](../handoff/test-file.local.v2.schema.json); both compiled at build time. |
| v2 rules | Done | [src/contract/rulesV2.ts](../../src/contract/rulesV2.ts): `ActivityPresenceRule`, `QuickCheckIdRule`, `VisualAssetRule`, `CalloutRule`, `SingleChoiceRule`, `MatchingRule`, `RecallRule`, `RapidVisualRule`. |
| Trig with explicit units | Done | `TrigNode` → `Sin/Cos/Tan/Asin/Acos/AtanNode` in [src/engine/expression.ts](../../src/engine/expression.ts). The answer-key check replays the fixture's `sin(30°)` problem (h = 2.0 m) with no warning. |
| Sphere, wireframe box, 2D highlight | Done | `SphereObject`, `BoxObject` wireframe mode, and `dg-highlight` in [src/render/](../../src/render/). |
| Asset guardrails | Done | 1.5 MB per image, 400 KB warning, 20 MB per file (D43). |
| `embed-image` | Done | [scripts/embed-image.ts](../../scripts/embed-image.ts), backed by [src/contract/imageInfo.ts](../../src/contract/imageInfo.ts) (PNG/JPEG detection and dimensions). |
| Placeholder proof fixture | Done | [docs/handoff/v0.3/placeholder-visual-checks.test.example.json](../handoff/v0.3/placeholder-visual-checks.test.example.json), generated with generic content (D49). It validates with **zero** issues and meets every SOW §7 minimum (checked by a unit test). |
| Guide | Done | [TEST_FILE_GUIDE.md §9](../TEST_FILE_GUIDE.md#9-v2-visual-rapid-checks): sets, the four item types, visuals, callouts, embedding pictures, trig, the checklist, and a v2 ChatGPT prompt. |

## SOW §3 rejections → tests

All in [tests/unit/contractV2.test.tsx](../../tests/unit/contractV2.test.tsx) (25 tests), each checking the exact JSON path and message:

| SOW §3 rejection | Test |
| --- | --- |
| Unsupported asset type | non-PNG/JPEG bytes; `image/svg+xml` media type; declared type ≠ real type |
| Missing alt text | missing field (schema) and blank text (rule) |
| Duplicate callout id | `CalloutRule` |
| Coordinate outside the canvas | `xPct: 120` → "Must be at most 100" |
| Duplicate option id | single choice (`SingleChoiceRule`) and matching term bank (`MatchingRule`) |
| Missing answer-map entry | `MatchingRule` |
| Reused correct label | `MatchingRule` |
| Label bank smaller than callout count | `MatchingRule` |
| Unsupported item type | schema enum |
| (also) remote URL / local path / `data:` prefix | schema pattern and `additionalProperties` |
| (also) missing `angleUnit`, no activities, recall without key, rapidVisual card without visual, duplicate set/item ids, oversize image/file | respective rules |

## Test totals

- Unit: **66** (41 original + 25 new), all passing.
- E2E (v1 regression): 7/7 in Chromium and Edge locally.

## Decisions

D39–D50 in [DECISIONS.md](../DECISIONS.md).

## Not in R0

The quick-check runner UI (R1) and the picture/callout rendering and delta E2E tests (R2). The placeholder fixture validates but can't be *run* until R1.
