# M1: Guided local runtime (complete)

**SOW gate:** timer, formula/basic-math/conversion dropdowns, calculation engine, variable naming/rename/reuse, review path.

| Gate item | Status | Evidence |
| --- | --- | --- |
| Timer | Done | `StudyTimer` state machine `ready → running ⇄ paused → finished` ([src/engine/timer.ts](../../src/engine/timer.ts)). Unit test with a fake clock; E2E **AC3** uses Playwright's clock (65 s → `01:05`, frozen while paused, frozen after finish). |
| Formula / basic math / conversion dropdowns | Done | [src/ui/AddStep.tsx](../../src/ui/AddStep.tsx): group → operation → one dropdown per input (Givens / Constants / My results) → name/symbol → Calculate. The only text inputs are the output name and symbol. E2E **AC5–7**. |
| Calculation engine | Done | `ExpressionNode` class tree ([src/engine/expression.ts](../../src/engine/expression.ts)) and `Operation` subclasses ([src/engine/operations.ts](../../src/engine/operations.ts)). No eval; the build CSP forbids it anyway. Unit tests cover every operator, arity, and error case. |
| Variable naming, rename, reuse | Done | `Variable` keeps a stable `var_####` id while name/symbol change ([src/engine/variables.ts](../../src/engine/variables.ts)). Auto names follow the SOW table (D21). E2E **AC6** renames a conversion result and selects it by its new name in a later step. |
| Review path | Done | [src/ui/Review.tsx](../../src/ui/Review.tsx): one step at a time or all, every teaching field, resolved inputs and results, `expectedLatex`, and a ✓ where her values match. E2E **AC9**. |
| Reset / new test | Done | `Session.resetProblem` clears one attempt; loading a file creates a new `Session`. E2E **AC10**. |
| Answer-key check (added) | Done | `AuthoredPath` replays each authored step with the learner's own `Operation` classes; `AuthoredValueRule` warns on mismatches (D5). All six fixture problems replay with no mismatch. |
| Download Attempt (optional in SOW) | Done | `AttemptExporter` → `guided-test-attempt.local/v1` JSON (D28). |

## Worked check against the fixtures

Every authored value in both fixtures matches the engine's recomputation. For example: 12·2000/3000 = 8 V; 0.5·24 = 12 V, then 6 W; A_b = 78.54 mm², A_eff = 628.32 mm², τ = 190.99 MPa; δ = 1 mm, ε = 5×10⁻⁴; γ_y = 0.003125, Δγ_p = 0.02, γ_r = 0.0185.

## Decisions made in this milestone

D1, D2, D5, D6, D13–D29 in [DECISIONS.md](../DECISIONS.md). Additions beyond the SOW: **Undo last step** (D20), non-blocking **unit hints** (D22), and the load-time **answer-key check** (D5).
