# Quick start (2 minutes)

1. **Open the app.** Double-click `index.html`. It opens in your web browser. No internet is needed.
2. **Load a test.** Click **Load Test** and choose the `.json` file Brent gave you, or drag the file onto the page. To try it first, click **ENGR 206** or **Mechanics of Materials** under *Use Included Examples*.
   - If the file has a problem, a red box lists what's wrong. Send that list to Brent.
3. **Pick a problem** from the list.
4. **Read the problem.** The prompt, givens, diagram and target are on the left. The formula sheet is below them. **Describe this picture** gives a text description of the diagram.
5. **Press Start.** The timer starts and **Add Step** unlocks. **Pause** stops the clock, and **Resume** continues it.
6. **Add a step:**
   1. Choose a type: **Formula sheet**, **Basic math**, **Unit conversion**, **Derivative**, or **Final answer**.
   2. Choose the formula or operation.
   3. Pick each input from its dropdown. Dropdowns list the givens, constants, and every result you've already calculated.
   4. Keep or change the suggested name and symbol for the result.
   5. Click **Calculate**. The app does the arithmetic and adds the result to *My work*. The result then appears in later dropdowns.
   - A yellow note means the units might not match (for example, kΩ where Ω is expected). It won't stop you, but it's worth checking.
   - **Rename** changes a value's name or symbol at any time. Earlier steps keep working.
   - **Undo last step** removes your most recent step.
7. **Finish.** Click **Finish and reveal path**. The timer stops for good, and the correct path appears. Reveal it **one step at a time**, or choose **Show all**. Each step explains why it comes next, what to notice, why this operation fits, what the inputs mean, and what the result unlocks. A green **✓ You reached this** marks the steps whose result you calculated.
8. **Try again.** **Reset problem** clears only that problem. **Download Attempt** saves your work as a file if you want to keep it.

Your work is kept only while the page is open. Closing the tab clears it.
